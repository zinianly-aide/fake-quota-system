@page "/"
@model "FakeQuotaSystem.Models.EmplQuota"
@inject IConfiguration Configuration

<div class="container">
    <h1 class="display-4">🏢 Fake Quota Management System</h1>
    <p class="lead">假勤额度管理系统</p>
    <p class="text-muted">.NET 8.0 + Blazor WebAssembly + Oracle Database</p>
    
    <div class="card">
        <div class="card-header">
            <h3>📊 额度概览</h3>
            <div class="card-body">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>区域</th>
                            <th>总应用数</th>
                            <th>活跃应用数</th>
                            <th>待审核数</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>4</td>
                            <td>100</td>
                            <td>15</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
        
        <div class="card">
            <div class="card-header">
                <h3>📋 额度预警</h3>
            <div class="card-body">
                <div class="alert alert-warning">
                    <strong>⚠️ 额度使用警告</strong>
                    <p>当前额度使用率已达到 85%</p>
                    <p>请及时添加新额度</p>
                </div>
            </div>
        </div>
        
        <div class="card">
            <div class="card-header">
                <h3>📋 快捷操作</h3>
            <div class="card-body">
                <button class="btn btn-primary">新建陪护假申请</button>
                <button class="btn btn-success">额度使用记录</button>
                <button class="btn btn-info">签核审核</button>
            </div>
        </div>
    </div>
</div>

@code {
    private string? _regionId;
    private int _totalApplications = 100;
    private int _activeApplications = 85;
    private int _pendingApprovals = 15;
}
