@page "/"
@model "FakeQuotaSystem.Models.Empvl"
@inject IConfiguration Configuration

<div class="container">
    <h1 class="display-4">📝 新增陪护假申请</h1>
    <p class="text-muted">创建新的陪护假类型和名称，申请相应的额度</p>
    
    <div class="card">
        <EditForm Model="FakeQuotaSystem.Models.EmplQuota" />
    </div>
    
    <div class="card">
        <h3>📋 申请表单</h3>
        <p class="text-muted">申请流程：</p>
        <ol>
            <li>选择陪护假类型（陪护假名称）</li>
            <li>选择区域（北京/深圳/北京护理/深圳护理）</li>
            <li>选择申请类型（年度/月度/时额度）</li>
            <li>选择天数（陪护假天数）</li>
            <li>填写备注信息</li>
            <li>提交申请（需要签核）</li>
            <li>等待审核批准</li>
            <li>额度添加到目标表</li>
        </ol>
        
        <div class="alert alert-info">
            <strong>💡 提示</strong>
            <p>所有申请都需要签核才能生效。签核完成后，额度会自动添加到目标表。</p>
        </div>
    </div>
</div>

@code {
    private string? _selectedRegionId;
    private string? _selectedApplicationType;
    private string? _selectedApplyType;
    private int _selectedDays = 7;
    private string? _remarks;
    
    private void SubmitApplication()
    {
        Console.WriteLine($"Submitting application: Region={_selectedRegionId}, ApplicationType={_selectedApplicationType}, Days={_selectedDays}");
        // TODO: Call API to submit application
    }
}
